# Vault의 핵심기능

1. 정적 비밀 저장(KV)
   - `/secret/data/{endpoint}`에서 저장되며, json 값이 모두 암호화되어 저장
   - 캐싱은 Vault Proxy(로컬 프로세스)로 지원(Enterprise)
2. 동적 비밀 저장
   - Database, AWS 등 외부 시스템에 접속하기 위한 credentials를 요청 시점에 동적으로 발급
   - 캐싱은 Vault Agent(로컬 프로세스)의 기능으로 지원
3. transit(OSS)
   - Vault가 지원하는 알고리즘으로 암호화 연산만 대행하고, 데이터 자체는 저장하지 않는다(Encryption as a Service)
   - 단방향/양방향 연산을 모두 지원한다.
      - 단방향(HMAC)
         1. `POST /transit/hmac/:name` 암호화 HMAC를 리턴
         2. `POST /transit/verify/:name`  valid 를 boolean으로 반환
      - 양방향(AES)
         1. `POST /transit/encrypt/:name`으로 암호화, `POST /transit/decrypt/:name`으로 복호화
         2. 암호화 결과는 Vault가 저장하지 않으므로, 호출자가 직접 자신의 DB 등에 보관해야 하며 복호화 시 그 ciphertext를 다시 Vault에 제출
         3. ciphertext는 `vault:v1:{base64}` 형태로 길이가 불규칙하고 원본과 포맷이 달라진다
4. transform(Enterprise)

   | 방식 | 동작 | 상태 저장 | 복원 |
   |---|---|---|---|
   | Tokenization | 원본 값을 그와 무관한 토큰으로 치환 | Vault내부에 평문과 토큰의 매핑을 암호화해 저장 | 가능 |
   | FPE | 원본과 동일한 형식/길이를 유지하며 암호화 | 매핑을 Vault에 저장하지 않음 | 가능 |

   - transit과의 차이는 vault에서 key가 탈취당하면 transit은 모두 복호화가 가능하지만, transform은 key탈취로 복원은 불가능하다(Tokenization)
   - Database 규격에 맞는 암호화가 필요할 경우 FPE를 사용해야한다(동일한 길이/형식 유지)
